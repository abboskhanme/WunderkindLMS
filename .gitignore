# Secrets / environment (HECH QACHON git'ga tushmasin)
.env
*.env
!.env.example

# Build output
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
*.user

# JS loyihasi (esproj) NuGet restore artefakti. Linux/mac'da papka nomi AYNAN
# `obj\Debug` bo'lib qoladi (Windows ajratuvchisi nom ichida), shuning uchun
# yuqoridagi `[Oo]bj/` qoidasiga tushmaydi.
schoollms.client/obj*

# Visual Studio
.vs/

# Node / client
node_modules/
dist/

# Uploads (runtime data)
SchoolLms.Server/uploads/

# Local secrets
appsettings.*.local.json

# Server statik (Docker build dist'dan yaratadi)
SchoolLms.Server/wwwroot/

# Baza backuplari (haqiqiy foydalanuvchi maʼlumotlari) — commit qilinmasin
backups/

# Demo akkaunt login/parollari — repoga tushmasin
tools/demo-credentials.txt

# Hisobot/artifakt HTML fayllari repoda saqlanmaydi
hisobot-*.html

# Mahalliy zaxira nusxalari — repoda saqlanmaydi
backups/

# Agent worktree papkalari — repozitoriyaga tushmasin
.claude/worktrees/

# EduSchool frontend bundle — tahlil uchun mahalliy nusxa, repozitoriyaga kirmaydi
.eduschool-bundle/
